apiVersion: apps/v1
kind: Deployment
metadata:
  name: e705-ui-deployment
  namespace: doa-e705-dev
  labels:
    app: e705-app
spec:
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: e705-app
  template:
    metadata:
      namespace: doa-e705-dev
    spec:
      serviceAccountName: sa-e705-front
      securityContext:
        runAsNonRoot: true
      automountServiceAccountToken: false
      containers:
        - name: e705-ui
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
          image: e705-ui
          imagePullPolicy: IfNotPresent
          env:
            - name: SERVER_PORT
              value: "8080"
            # Debug
            - name: USER_ID
              value: "$(id -u)"  # This will show what UID is actually being used
          envFrom:
            - configMapRef:
                name: e705-ui-config
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          resources:
            limits:
              memory: 2Gi
            requests:
              cpu: 100m
              memory: 256Mi
              ephemeral-storage: "2Gi"
