apiVersion: v1
kind: Service
metadata:
  name: e705-ui-service
  namespace: doa-e705-dev
  labels:
    app: e705-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: e705-app
